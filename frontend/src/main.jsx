import React from "react";
import ReactDOM from "react-dom/client";
import PatentApp from "./PatentApp.jsx";
import "../style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PatentApp />
  </React.StrictMode>
);
